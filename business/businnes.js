//importation des modules


const express = require("express");
const buisness = require("../data/data")

// Initisation de l'application 


// Fonctions à exporter 

const buisness_public {
    get_all_users : ( ) => {
        res.
    },
    add_users : () => {
        
    },
        edit_users : ( ) => {
        
    },
    delete_users : ( ) => {
        
    }
};


// Exportation des module
module.exports = buisness_public; 